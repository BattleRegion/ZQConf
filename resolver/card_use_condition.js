const Base = require('./base');
class EnemyGacha extends Base {
    constructor(opt){
        super(opt);
    }

    specDeal(){
        // console.log(this.confData);
    }
}

module.exports = EnemyGacha;